{
  "name": "celeb-api-gateway",
  "version": "1.0.0",
  "description": "API Service for SPIN Protocol Celeb Project on AWS API Gateway",
  "scripts": {
    "deploy": "sls deploy -v --stage $STAGE",
    "remove": "sls remove -v --stage $STAGE"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "serverless-domain-manager": "^3.1.0"
  },
  "dependencies": {}
}
